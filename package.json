{
	"name": "setest",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --filter='*' dev",
		"build": "cd packages/shared && bun run build && cd ../.. && bun run --filter='*' build",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"typecheck": "bun run --filter='*' typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.3",
		"typescript": "^5.9.2",
		"@types/bun": "latest"
	},
	"module": "index.ts",
	"type": "module"
}
